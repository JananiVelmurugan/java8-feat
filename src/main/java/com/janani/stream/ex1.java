package com.janani.stream;

public class ex1 {
	public static void main(String[] args) {
		// Using streams for processing database results
		// funConnection.queryToStream("/* SQL HERE */").filter(it ->
		// it.get("firstName").equals("Robert"))
		// .collectors(Collectors.summarizingInt(it -> it.getSalary()));
	}
}
